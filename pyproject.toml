[project]
name = "dj-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=6.0.3",
    "psycopg[binary]>=3.3.3",
    "python-dotenv>=1.2.2",
]
